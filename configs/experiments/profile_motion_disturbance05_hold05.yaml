# Task inputs, resource budgets, and explicit differences from the shared parameters.
parameters: configs/parameters.yaml
grippers: [configs/grippers/piper.yaml]
manifest: configs/objects/bottle_matryoshka.yaml
cache: outputs/prepared
output: outputs/profile_motion_disturbance05_hold05
grasp_regions: annotations/grasp_regions
device: 1
environments: 8
seed: 20260908
target_successes: 16
candidate_budget: 1024
time_budget_s: 3600
objects: [bottle]
sampling:
  surface_samples: 4096
  surface_rounds: 16
  normal_alignment_min: 0.9
validation:
  random_directions: 1
  hold_s: 0.5
  final_hold_s: 0.5
  max_translation_m: 0.003
  max_rotation_rad: 0.1
  disturbance_acceleration_m_s2: 0.5
