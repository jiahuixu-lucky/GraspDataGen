# 当前状态与剩余工作

更新：2026-09-09。本文保留实际验收结论、未解决问题和后续门槛；安装、
物理/坐标契约和适配流程分别见 [安装](installation.md)、[数据契约](contracts.md)
和 [夹爪适配](grippers.md)。阶段调试脚本、重复过程文档和过期日志已清理。

目录清理后，历史 `outputs/` 数据和诊断文件已删除。下文数量和结果属于
此前运行记录，不代表当前目录仍有这些数据。正式入口统一为
`configs/runs/production.yaml`。2026-09-21 默认物体清单已切换为
`configs/objects/our_assets.yaml`，数据写入 `outputs/our-assets/`，准备资产写入
`outputs/prepared/`；旧的 smoke 配置与根目录缓存不再使用。

## 状态

| 阶段 | 状态 |
| --- | --- |
| P0 环境与原生 API | Python 3.12 / Isaac Sim 6.0.1 原生 GPU PhysX 路径已验证 |
| P1 资产与夹爪定义 | Piper、ARX-X5、瓶子与套娃 00002 已完成真实准备和标定 |
| P2 最小物理闭环 | 历史数据四组合各 16 条，全部通过新进程单环境三次完整试验；反例及敏感性结果已保存 |
| P3 批量生产 | 补充采样、预算、去重和恢复已实现；完整生产与恢复验收未完成 |
| P4 性能与稳健性 | 试验重置导致的单／多环境差异已修复；完整性能、精度敏感性与稳健性验收未完成 |
| P5 替换与清理 | 默认环境、公开入口和旧链路清理已完成；完整数据验收仍未通过 |

P5 按用户要求先于 P3/P4 实施，不能据此把前两阶段标为完成。
旧环境不再作为运行入口，机器人源资产从外部 `Assets/` 读取，默认物体源资产
从 `Data/our_Assets/` 读取。下述历史验收结论不代表新物体清单已完成生产验收。

## 历史数据与已知问题

`outputs/smoke` 由空准备缓存生成，采用 xyz/xyzw 姿态、直立取物约束及三次完整试验。
生成时各组合保存 16 条；检查通过所有分片校验、唯一 ID、姿态去重和底面间隙。
已完成任务的恢复曾验证 manifest 与分片不变。删除开发模块后实现身份发生变化，
历史任务不能直接按新代码恢复；新生成任务应使用新的输出目录，已有数据仍可回放。

| 组合 | 验证候选数 | 保存数 | 修复前 16 环境重放 | 修复后 16 环境重放 |
| --- | ---: | ---: | ---: | ---: |
| Piper / bottle | 64 | 16 | 16/16 | 16/16 |
| Piper / matryoshka 00002 | 64 | 16 | 15/16 | 16/16 |
| ARX-X5 / bottle | 128 | 16 | 16/16 | 16/16 |
| ARX-X5 / matryoshka 00002 | 64 | 16 | 16/16 | 16/16 |

修复前，Piper / 套娃候选 `676920072347365623` 在第三次试验的翻转后保持阶段失败：
末尾连续稳定时间为 0.125 s，要求为 0.2 s。双指仍有接触，最大位移约 0.145 mm、
旋转约 0.00841 rad，未超过相应限制。同一候选在单环境三次试验均通过。
其余三个组合各一条固定样本也通过了单环境三次试验；这不等同于全数据单环境验收。

2026-09-09 的逐步对照定位到重置只写位置／速度、没有清除 PhysX 内部历史状态。
公共重置现已在后续试验及批次前重建场景，保留 audit 的质量／摩擦倍率。
原失败候选修复后的单／16 环境三轮共 7,059 步，物体姿态、关节位置、
物体速度及接触力一致，三轮翻转后末尾稳定时间均为 1 s。正式 CLI 四组合
批量重放均完成并通过，未更改任何物理阈值。详见[一致性诊断](consistency.md)。
Piper／套娃 00002 的全部 16 条另已通过新进程单环境三轮完整回放；按 ID
比较，输入、实际闭合姿态和全部阶段指标均与批量一致，报告完成且无物理错误。
另用同一候选的 64 个隔离副本复现原生成规模，三轮全部通过，所有逐步状态
与单环境一致；副本仅用于诊断，不计入数据量。单指／重力滑落反例各三轮
正确拒绝，两个夹爪的质量／惯量与摩擦倍率在重复重建后读取正确。

修复前，同一套娃批次的精度诊断将步长由 1/120 s 改为 1/240 s，位置/速度迭代数由
64/4 改为 128/8，保持物理参数、阶段时间、阈值和随机条件不变，结果为 14/16。
原失败候选通过；`13145211491509363` 闭合不稳，`999580660811250055` 扰动失败，
各发生三次。PhysX 对 TGS 超过四次速度迭代有行为变化警告；该诊断同时改变多个
求解设置，不能认定为修复或单一原因的证明。运行时还有其他 GPU 任务，未作隔离性能测量。

P3 曾生成 233 条开发数据：Piper / bottle 105 条、Piper / 套娃 00002 128 条，
其 `worker_verified` 当时仍为 false，不能作为完整验收数据。全部物体的生产清单含
瓶子和 25 个套娃。已有准备记录中，00000、00001、00010、00011、00012 超出
旧版 1.5 mm 碰撞代理误差预算；00010 另有 GPU 碰撞回退警告。这些是历史记录。
当前已移除项目 CoACD 分解和表面误差预算，沿用源资产的 PhysX 碰撞设置，
旧预算失败不再构成资产拒绝条件。
当前 ARX 摩擦模型为 0.8，历史 P2 为 0.5，两者不能混作同一物理配置。

## 原生碰撞路径验证（2026-09-09）

正式生产现已改为每个候选一轮完整试验；下面的三轮记录属于修改前的验证结果。

生产清单的 26 个物体已全部完成真实 GPU 准备，质量、惯量、材料及 USD 引用检查通过，
未出现 PhysX 错误或碰撞回退日志。直接保留源碰撞网格和属性，采样使用其原生烘焙结果。
瓶子和套娃 00000 的重载碰撞表面与源烘焙结果差异为 0。
这项检查核对保存前后是否一致，不是已移除的视觉表面误差预算。

Piper、ARX-X5 分别搭配瓶子和套娃 00000，四组均以 16 环境执行生成，
每组尝试 16 个候选并保存目标 1 条成功数据；生产报告 `all_targets_reached=true`。
四条数据分别在新进程、单环境下完成全部三轮回放，均通过且无 PhysX 错误。
本次结果在 `/tmp/graspdatagen-native-check/`，正式准备资产在 `outputs/prepared/`。
这覆盖了原生碰撞的准备、生成和回放链路，不代表 512 环境或完整 52 组合长跑验收。

## 后续验收

1. 在独立试验重置下重新评估步长与求解设置敏感性，剔除不稳定候选；保留失败结果，不能仅挑选
   通过的重跑，也不能放宽物理判据来达到数量目标。
2. 将新进程单环境完整重放扩展到其他三个组合及新增生产数据；Piper／套娃
   00002 的 16 条已完成。继续在相同候选 ID、协议、资产与随机条件下比较
   单/多环境结果，再做步长与求解精度对比。
3. 完成两个夹爪与全部物体的准备和生产；每组合目标为 1,024 条去重后有效抓取，
   预算不足或资产不合格必须明确报告，不能复制近邻姿态或修改尺度凑数。
4. 完成真实中断、提交前后恢复与并发写入排他检查，核对已提交分片不丢失、不重复，
   源依赖或配置变化使任务身份失效。
5. 在同协议同资产下测量环境数阶梯、吞吐、显存、阶段通过率；连续运行至少一小时，
   检查预热后资源稳定和 contact buffer 错误，报告质量/摩擦变化的敏感性。

所有 GPU 命令在沙箱外执行；以真实资产、生成、存储和回放验证，不编写单元测试。
公开命令为 `graspdatagen inspect / prepare / generate / replay / audit`。
运行配置保留 `production.yaml` 与 `audit.yaml`。

## 历史证据路径（已清理）

- `outputs/p2-acceptance.json` 及其中列出的文件：历史验收数据、重放、反例和敏感性。
- `outputs/smoke/`：生成数据、配置快照、输入身份及正式报告。
- `outputs/p5-replay/`、`outputs/p5-serial/`、`outputs/p5-precision/`：当前问题的原始报告和数组。
- `outputs/consistency/`：重置缺陷的逐步对照、修复后正式回放及反例检查。
- `outputs/p3/preparation.json`：全部物体准备的已知失败及中断位置。
- `outputs/p3-qualification/`：尚未通过验收的已提交开发数据及恢复状态。

以上路径供理解历史运行记录，当前不再可用于回放或重算验证报告。
后续验收必须重新生成数据并保留相应准备资产及运行报告。
