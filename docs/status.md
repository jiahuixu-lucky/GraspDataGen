# 已知限制

当前夹爪为 Piper 和 ARX-X5，`configs/objects/our_assets.yaml` 包含 25 个物体。小规模准备、生成和独立回放链路已验证，完整生产验收尚未完成。

- **齿轮无有效候选：** 3 mm 厚度与当前接触位置、底面间隙要求冲突，需要调整采样。
- **静态检查未全通过：** 区域标注工具和 Web 入口还有导入排序及类型标注问题。
- **待验收：** 全物体生成与回放、单／多环境结果一致性、物理参数敏感性、中断恢复和长时间资源稳定性。

生成命令完成不等于达到抓取目标；检查报告中的 `all_targets_reached` 和各组合的 `stop_reason`。
