# 状态与待办

当前使用 Piper、ARX-X5 和 `configs/objects/our_assets.yaml` 物体清单。
默认生产目标为每组合 1,024 条独立成功抓取，完整生产验收尚未完成。

历史小规模运行已覆盖准备、生成、独立回放和物理反例；原报告与诊断数据
已清理，不作为当前物体清单的验收依据。试验重置现会重建物理场景，避免
PhysX 历史状态影响后续试验；这不保证任意布局下逐位确定性。

待完成：

- 全物体生产及新进程单环境回放，并比较同一候选的单／多环境结果。
- 步长、求解精度、质量和摩擦敏感性验证。
- 真实中断恢复、分片完整性和并发写入排他验证。
- 环境数阶梯性能测试及至少一小时运行的资源稳定性检查。

已记录的采样问题：齿轮厚度仅 3 mm，当前指垫内部接触构型与夹爪高于
物体底面 3 mm 的要求冲突，候选全部被底面间隙筛选拒绝。
需要调整接触位置采样，仅增加轮数无效。
